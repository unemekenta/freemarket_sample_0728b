- if user_signed_in?
  = link_to new_product_path do
    .sell-btn
      .sell-btn__text
        %div 出品
      .sell-btn__icon-camera
        = fa_icon 'camera'
- else
  = link_to new_user_session_path do
    .sell-btn
      .sell-btn__text
        %div 出品
      .sell-btn__icon-camera
        = fa_icon 'camera'